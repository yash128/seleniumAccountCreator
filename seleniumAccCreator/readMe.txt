This is hack which can be used to generate money. It automatically visits page that has ad on it in peakmeIn.java file. It reloads wifi from its admin panel so that the ip changes and we could have a unique impression.

This also have subscribe.java. Which signs google and subscribe to video given. It uses selenium which worked perfectly.

It can also create yandex(yahoo) mail accounts automatically and saves all the provided info in .txt file. This was done to make sure we have all info and any verification process if added my server, we can provide with correct date of birth etc.